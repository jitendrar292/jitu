{
    "script" : {
        "source": "for (int i = 0; i < ctx._source.offices.length; ++i) { if(ctx._source.offices[i].officeId == params.officeId) { ctx._source.offices[i].contacts.add(params.contact)}}",
        "lang": "painless",
        "params" : {
            "officeId": null,
            "contact": null
        }
    }
}